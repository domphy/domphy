import { confettiButton } from "@domphy/blocks";

// confetti() alone is a fixed full-viewport canvas (empty in cell shots).
// The button variant is the resting catalog UI and still fires a burst on click.
export default confettiButton();
