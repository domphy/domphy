// Breakpoint below which the playground's split (code | preview) layout
// collapses into stacked Code/Preview tabs. Keyed to the playground's own
// rendered width via a CSS container query — not the viewport — so it
// degrades correctly even when a narrow docs sidebar+TOC column squeezes the
// available space well below the viewport width.
export const PLAYGROUND_STACK_QUERY = "@container (max-width: 40em)" as const;
