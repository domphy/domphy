// Sibling array with a duplicated _key — only detectable when the array is
// diagnosed as one unit, not split into per-element diagnoses.
export const items = [
  { div: "a", _key: 1 },
  { div: "b", _key: 1 },
];
